public boolean college(int avgTemp, int numStudents, boolean csMajor){
    //declare lovcal boolean variable

     if(avgTemp <= 80)
        //do something here
     if(numStudents <= 8000)
        //do something here
     if(!csMajor)
        //do something here

   //return local variable
    return ________;
}
